use super::PhysicalRect;

pub(crate) fn update_maximize_button_rect(_rect: PhysicalRect) -> Result<(), String> {
    Ok(())
}

pub(crate) fn install_windows_chrome(_window: &tauri::WebviewWindow) -> Result<(), String> {
    Ok(())
}
